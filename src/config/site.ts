export const siteConfig = {
  // Global flag to enable or disable copy protection
  // When true: disables text selection, right-click context menu,
  // drag events, and copy/paste/view-source keyboard shortcuts.
  enableCopyProtection: true,
};
